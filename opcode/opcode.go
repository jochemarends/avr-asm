package opcode

const (
    NOP := 0x0000
)

encode(opcode.NOP)
encode(opcode.NOP)

encode(opcode.ADDW, 1, 3)

encode(opcode.ADDW, 1, 3)

func encodeADDW(Rd, Rr) {

}

fmt.Print(operands.MOVW) // Rd, Rr
fmt.Print(opcodes.MOVW)  // 0000 0001 dddd rrrr

func encodeMOVW(...) int16 {
    
}

fmt.Print(opcodes.MOVW) 

type Operand int16

Operand.Where().

RegisterPair.In(R24, R26, R28, R30)
RegisterPair.

const (
    Byte = iota
    Word
)

type Register interfac

